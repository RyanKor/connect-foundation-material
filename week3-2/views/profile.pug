extends layout

block content
    .manage
      .followings.half
        h2 Following
        if user.Followings
          for following in user.Followings
            div= following.nick
      .followers.half
        h2 follower
        if user.Followers
          for follower in user.Followers
            div= follower.nick